<!-- Floating Button Container -->
<div id="floating-buttons" class="fixed bottom-5 right-5 flex flex-col items-center space-y-3 group z-50 mt-3">
    
    <!-- WhatsApp Button -->
    <button id="whatsapp-button" class="relative flex items-center justify-center bg-green-500 text-white text-sm font-semibold rounded-full shadow-lg transition-all duration-300 lg:w-40 lg:h-12 w-12 h-12">
        <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg" 
             alt="WhatsApp" 
             class="h-6 w-6">
        <span class="ml-2 hidden lg:inline">Any Queries?</span> <!-- Text only visible on large screens -->
    </button>

    <!-- Home Tuition Button -->
    <a href="/student" id="tuition-button" class="relative flex items-center justify-center bg-blue-500 text-white text-sm font-semibold rounded-full shadow-lg transition-all duration-300 lg:w-40 lg:h-12 w-12 h-12">
        <img src="https://cdn-icons-png.flaticon.com/512/2991/2991126.png" 
             alt="Home Tuition" 
             class="h-6 w-6">
        <span class="ml-2 hidden lg:inline">Enroll Now</span> <!-- Text only visible on large screens -->
    </a>
</div>

<!-- CSS for Responsive Behavior -->
<style>
    /* On small and medium screens, hide text and make buttons circular */
    @media (max-width: 1024px) {
        #whatsapp-button span, #tuition-button span {
            display: none;
        }
        #whatsapp-button, #tuition-button {
            width: 48px;  /* Make buttons circular */
            height: 48px;
        }
    }
</style>
